export class Breadcrumb {
    icon: string;
    title: string;
    link: string;

    constructor(icon: string, title: string, link: string) {
        this.icon = icon;
        this.title = title;
        this.link = link;
    }
}
